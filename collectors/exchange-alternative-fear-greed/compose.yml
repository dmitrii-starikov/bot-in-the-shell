services:
  exchange-alternative-fear-greed:
    build: .
    container_name: ${COMPOSE_PROJECT_NAME}-exchange-alternative-fear-greed
    env_file:
      - ../../.env
      - .env
    volumes:
      - ./tmp:/app/tmp
    networks:
      - bot-network

networks:
  bot-network:
    name: ${COMPOSE_PROJECT_NAME}-network
    external: true
