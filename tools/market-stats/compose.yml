services:
  market-stats:
    build: .
    container_name: ${COMPOSE_PROJECT_NAME}-market-stats
    env_file:
      - ../../.env
      - .env
    networks:
      - bot-network

networks:
  bot-network:
    name: ${COMPOSE_PROJECT_NAME}-network
    external: true
